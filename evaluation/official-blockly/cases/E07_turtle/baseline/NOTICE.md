# Upstream notice

The files under `upstream/` are unmodified, line-range extracts from
`google/blockly-games` at commit
`5d2ff6f39207f7ed31e3d11ba1de277c85efe4c2`. They retain the upstream
Apache-2.0 licence. Exact paths, ranges and SHA-256 hashes are recorded in
`baseline-extraction.json`.
